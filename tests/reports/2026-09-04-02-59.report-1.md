# 测试报告：Phase 16 云端数据快照持久化

- 测试时间：2026-09-04 02:49 ~ 02:59
- 测试范围：`src/stock_plan/data/snapshot.py`、`tests/test_snapshot.py`、`app.py` 启动恢复钩子、`update_center.py` 云端集成、`scripts/publish_snapshot.py` 发布流程、远端快照分发
- 执行命令：
  - `$env:PYTHONPATH='d:\Vscode\stock plan\Text\src'; .\.venv\Scripts\python.exe -m pytest tests -q`
  - `.\.venv\Scripts\python.exe scripts\publish_snapshot.py`（首份快照发布）
  - `Invoke-WebRequest https://raw.githubusercontent.com/Cwywwy/Text-stock/data-snapshot/manifest.json`

## 结果

| 项 | 结果 |
| --- | --- |
| pytest 全量 | **31/31 通过**（含新增 9 个快照用例：part_filename、manifest 校验、build/拼卷 roundtrip、is_cloud 判定） |
| 模块导入检查 | 通过（app / update_center / snapshot，bare mode 警告可忽略） |
| 首份快照发布 | 成功：5553 只 / 299MB / 4 卷 → `data-snapshot` 分支 |
| 远端分发验证 | manifest.json HTTP 200；part03 HEAD HTTP 200（30,227,504 字节） |
| 计划任务 | `\StockPlan\Snapshot_2130` 注册成功，下次运行 2026-09-04 21:30 |
| 浏览器回归 | 今日信号页正常；数据更新页显示「本机完整模式」徽章 + 「从云端快照恢复」入口 + 5553 只指标 |

## 失败与修复记录

1. roundtrip 测试 2 次失败：manifest.parts 结构（dict 列表）与 validate 返回值（问题列表）与测试初稿假设不符 → 修正测试对齐实现。
2. 首次发布时 `subprocess` 读 git 输出 GBK 解码崩溃（Windows 默认编码）→ snapshot.py `subprocess_run` 加 `encoding="utf-8", errors="replace"` 后发布成功。
3. schtasks 注册因路径空格 + 非提权会话两次失败 → 经 UAC 提权 + XML 模板注册成功。

## 数据缩减说明

不适用（单测使用 3 只假 parquet 小样本，仅为打包逻辑验证，非真实数据测算）。

## 本地验证链接

- http://localhost:8503 （启动命令：`.\.venv\Scripts\python.exe -m streamlit run src\stock_plan\ui\app.py --server.port 8503 --server.headless true`）
