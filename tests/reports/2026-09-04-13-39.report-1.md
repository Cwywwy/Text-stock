# 测试报告：云端回测/信号 OOM 修复

- **测试时间**：2026-09-04 13:39
- **测试范围**：`Storage.load_market_maps` 新方法单测 + 全量 pytest 回归 + 受影响 4 页 AppTest 冒烟
- **测试环境**：Windows，Python 3.13.15（uv 管理，`.venv`），pytest 8.x
- **测试命令**：
  - `.venv\Scripts\python.exe -m pytest tests -q`
  - `AppTest.from_file(src/stock_plan/ui/views/{backtest,compare,visual_builder,today}.py)`

## 结果

| 项目 | 数量 |
| --- | --- |
| 通过 | 41 |
| 失败 | 0 |

- 新增 `tests/test_storage_market_maps.py` 11 个用例：日期窗口过滤、lead_days 预热、全历史回退、板块筛选、ST 剔除、max_codes 上限与不生效、fund_map 跟随、空股票列表、窗口无数据。
- 首轮 1 个失败（`test_boards_filter` 预期错误：`000001` 深主板也属主板板块），修正测试预期后全部通过；产品代码无改动。

## 受影响页面 AppTest

| 页面 | 异常数 |
| --- | --- |
| backtest | 0 |
| compare | 0 |
| visual_builder | 0 |
| today（信号） | 0 |

## 数据缩减说明

单测使用 4 只股票 × 约 430 个交易日的合成数据（临时目录注入 Storage），不依赖真实缓存。

## 本地验证链接

本次为纯后端/数据层修复，未启动本地服务；如需人工验证可运行 `uv run streamlit run app.py --server.headless true` 后访问 `http://localhost:8501` 的回测/对比/拼装/今日信号页。
