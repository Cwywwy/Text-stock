# 新浪日线 MiniRacer 崩溃修复测试报告

- 测试时间：2026-09-04 23:08（UTC+08:00）
- 测试范围：AkShare 新浪日线请求的并发互斥、增量更新逻辑、全量 Python 回归测试。

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `uv run pytest tests\test_fetcher.py tests\test_updater.py -q` | 6 passed（1.21s） |
| `uv run pytest -q` | 52 passed（1.93s） |
| `uv run python -c "...get_daily_bars('600000', '20260901', '20260904')..."` | 通过：返回 4 条日线，字段完整 |
| `git diff --check` | 通过 |

- 通过：52 项 pytest 测试，1 项真实新浪日线读取
- 失败：0

## 覆盖与限制

- 正向：四个并发请求均返回标准化日线 DataFrame。
- 并发：测试桩记录的 AkShare 调用峰值并发数为 1，证明 V8 解码器入口已互斥。
- 错误场景：不执行真实 AkShare 网络请求或真实 V8 初始化，以避免测试环境触发原生 DLL 崩溃。
- 数据缩减：使用 4 个模拟股票代码，而非全 A 股真实请求；目的仅验证并发互斥，不验证行情供应商数据。

## 部署限制

修复当前位于 `agents/branch1-data-update` 工作树。Windows 计划任务实际执行目录 `D:\Vscode\stock plan\Text` 必须先纳入该修复，之后 `Update_0800` 与 `Update_2000` 才会使用安全的日线请求路径。

本轮不涉及 Web 服务，因此无本地验证链接。
