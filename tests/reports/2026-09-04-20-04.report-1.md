# 测试报告：共享策略发布与全市场信号

- **测试时间**：2026-09-04 20:04
- **测试环境**：Windows、Python 3.13.15、项目 `.venv`
- **测试范围**：共享策略登记、配置指纹、待发布状态、GitHub 文件缺失处理、本机发布入口、云端信号读取、公开确认 UI、云端回测提示

## 执行命令

```powershell
.venv\Scripts\python.exe -m py_compile src\stock_plan\data\snapshot.py src\stock_plan\strategy\publication.py src\stock_plan\ui\app.py src\stock_plan\ui\views\today.py src\stock_plan\ui\views\llm.py src\stock_plan\ui\views\visual_builder.py src\stock_plan\ui\views\backtest.py src\stock_plan\ui\views\compare.py scripts\publish_snapshot.py
.venv\Scripts\python.exe -m pytest tests -q
```

并通过 `streamlit.testing.v1.AppTest` 对 `backtest`、`compare`、`visual_builder`、`today`、`llm` 页面进行冒烟测试。

## 结果

| 项目 | 结果 |
| --- | --- |
| 已修改 Python 文件编译 | 通过 |
| pytest | 49 通过，0 失败 |
| AppTest（5 页） | 5 通过，0 异常 |

新增 `tests/test_strategy_publication.py` 覆盖 8 个场景：配置指纹稳定性与变化、合法/非法名称、同名拒绝、待发布记录及远端文件不存在。

## 数据缩减说明

测试使用网络请求替身和小型策略配置，不执行真实 GitHub 写入，也不扫描全 A 股数据。全市场扫描只会在本机实际待发布策略任务中执行。

## 部署前人工配置

在 Streamlit Community Cloud 的 Secrets 中配置 `DATA_SNAPSHOT_TOKEN`，令牌仅授予 `Cwywwy/Text-stock` 仓库 Contents 读写权限。令牌未写入代码或测试报告。

## 本地验证链接

未启动常驻服务。人工验证可执行：

```powershell
uv run streamlit run src/stock_plan/ui/app.py
```

随后访问 `http://localhost:8501`。本机启动会检查并异步触发待发布公共策略任务。

